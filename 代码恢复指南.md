# 代码恢复指南

## 1. 检查 Git 历史记录

### 查看所有分支的提交历史
```bash
git log --all --oneline --graph -30
```

### 查看详细的 reflog（可能包含已删除的提交）
```bash
git reflog --all -50
```

### 检查是否有未提交的更改被保存
```bash
git fsck --lost-found
```

## 2. 恢复丢失的提交

### 方法 1: 从 reflog 恢复
```bash
# 查看 reflog 找到丢失的提交
git reflog

# 恢复特定提交（替换 COMMIT_HASH）
git checkout COMMIT_HASH
git checkout -b recovered-code

# 或者直接重置到该提交
git reset --hard COMMIT_HASH
```

### 方法 2: 检查所有分支
```bash
# 列出所有分支（包括远程）
git branch -a

# 检查每个分支的差异
git log --all --oneline --graph
```

## 3. 检查 Cursor 的本地历史

Cursor 可能会保存本地编辑历史：
- 查看 `.cursor/` 目录
- 检查是否有自动保存的文件

## 4. 检查是否有 stash

```bash
# 列出所有 stash
git stash list

# 如果有 stash，可以恢复
git stash pop
# 或
git stash apply stash@{0}
```

## 5. 恢复已删除的文件

```bash
# 查找已删除的文件
git log --diff-filter=D --summary

# 恢复特定文件（替换 FILE_PATH 和 COMMIT_HASH）
git checkout COMMIT_HASH -- FILE_PATH
```

## 6. 检查工作目录状态

```bash
# 查看所有更改（包括未跟踪的文件）
git status -u

# 查看是否有未跟踪的重要文件
git clean -n -d
```

## 7. 如果代码从未提交

如果代码从未提交到 git，可以尝试：
- 检查 Cursor 的本地历史记录
- 检查系统临时文件
- 检查是否有自动备份

## 紧急恢复步骤

1. **立即停止所有操作**，避免覆盖更多数据
2. **运行恢复脚本**：`bash 代码恢复脚本.sh`
3. **检查 reflog**：`git reflog --all`
4. **创建恢复分支**：`git checkout -b code-recovery`

## 针对当前情况的诊断结果

根据检查，您的 Git 仓库状态正常：
- ✅ 工作目录干净，没有未提交的更改
- ✅ 没有发现悬空对象或损坏的提交
- ✅ 当前分支 `cursor/cursor-code-loss-investigation-7a18` 与 master 分支一致

### 可能的情况和解决方案

#### 情况 1: 特定文件的代码丢失
如果您知道是哪个文件丢失了代码：
```bash
# 查看文件的历史记录
git log --all --full-history -- <文件路径>

# 恢复到特定版本
git checkout <commit-hash> -- <文件路径>
```

#### 情况 2: 未提交的更改丢失
如果代码从未提交：
- Cursor 编辑器可能有本地历史记录（检查编辑器设置）
- 检查系统临时目录：`/tmp` 或 `~/.cache`
- 如果使用了 IDE，检查是否有本地历史功能

#### 情况 3: 代码在另一个分支
```bash
# 检查所有分支
git branch -a

# 切换到其他分支查看
git checkout <分支名>
```

#### 情况 4: 代码被意外重置
```bash
# 查看 reflog 找到之前的 HEAD 位置
git reflog

# 恢复到之前的提交
git reset --hard HEAD@{n}  # n 是 reflog 中的索引
```

## 快速恢复命令参考

```bash
# 1. 查看所有可能的恢复点
git reflog --all -50

# 2. 搜索包含特定关键词的提交
git log --all --oneline --grep="关键词"

# 3. 恢复特定文件的所有历史版本
git log --all --full-history -- <文件路径>

# 4. 检查是否有备份分支
git branch -a | grep backup
git branch -a | grep recovery

# 5. 查看最近修改的文件
git log --all --name-only --oneline -20
```
